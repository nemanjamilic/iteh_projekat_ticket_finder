export interface PieChartProps {
    title: string,
    value: number,
    series: Array<number>,
    colors: Array<string>
}

export interface SatisfactionBoxProps{
    title: string,
    value: number
}